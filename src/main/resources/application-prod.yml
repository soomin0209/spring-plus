spring:
  config:
    import: aws-parameterstore:/spring-plus/prod/
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never

jwt:
  secret:
    key: ${JWT_SECRET_KEY}